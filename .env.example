# API base URL (no trailing slash).
# Dev: leave unset to use Vite proxy (/api → localhost:3000).
# Production: set to your backend URL, e.g. https://api.yourdomain.com or https://yourdomain.com/api
VITE_API_BASE_URL=https://your-backend-api-url.com
